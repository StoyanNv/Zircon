@model AddCategoryBindingModel
@{
    ViewData["Title"] = "Добави категория";
}

<h2>@ViewData["Title"]</h2>
<form asp-area="Admin" asp-controller="Products" asp-action="AddCategory" method="post" class="form-horizontal">
    @Html.EditorForModel()
    <div class="form-group">
        <div class="col-sm-10 col-sm-push-2">
            <input type="submit" value="Създай" class="btn btn-success" />
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="btn bg-warning btn-default">Прекъсни</a>
        </div>
    </div>
</form>
